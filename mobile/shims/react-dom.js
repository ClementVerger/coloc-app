// Stub for react-dom — not available in React Native.
// @clerk/clerk-react imports it but only uses it in web-only code paths
// (useCustomElementPortal) that never execute in a native environment.
module.exports = {};
